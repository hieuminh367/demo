# include <iostream>
using namespace std;

int main(){
	float a,b,c;
	cin >> a >> b >> c;
	if (a+b>c){
		if ( a ^ 2 + b ^ 2 = c ^ 2){ //|| a*a + c*c = b*b || b*b + c*c = a*a ){
			cout << "tam giac vuong";
		}	
		if ( a = b ){ //|| a = c|| c = b ){
			cout << "tam giac can";
		} 
	}
			
		       
		
	
}
